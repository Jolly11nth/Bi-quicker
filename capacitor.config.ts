import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.biquicker.app',
  appName: 'Bi-quicker',
  webDir: 'dist',
  android: {
    backgroundColor: '#ffffff',
  },
  ios: {
    backgroundColor: '#ffffff',
  },
}

export default config
